[package]
name = "rl-std-macros"
version = "1.0.0"
readme = "readme.md"
edition.workspace = true
license.workspace = true
description = "Procedural macros for the rl-lang standard library: #[native_fn] lowers a Rust function into a thin-pointer native descriptor plus its checker signature"
repository.workspace = true

[lib]
proc-macro = true

[dependencies]
syn.workspace = true
quote.workspace = true
proc-macro2.workspace = true
