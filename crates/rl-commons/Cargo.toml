[package]
name = "rl-commons"
version.workspace = true
readme = "readme.md"
edition.workspace = true
license.workspace = true
description = "Common types shared across rl-lang crates to break dependency cycles"
repository.workspace = true

[dependencies]
rl-ast.workspace = true
rl-std-core.workspace = true
# Signature data for the migrated modules is sourced from `rl-std` (default
# `signatures` feature = no OS-facing deps), so the checker/LSP stay light.
rl-std.workspace = true
